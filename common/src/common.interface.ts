export interface IHelloRespond {
  message: string;
}

export interface IUserInfoResponse {
  userId: string;
  accessToken: string;
  refreshToken: string;
  userName: string;
  avatarUrl: string;
}
